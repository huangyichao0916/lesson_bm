// const initialState = {
//     addedIds : [],
//     quantityById: {},
// }

const products = (state = [] , action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default products;