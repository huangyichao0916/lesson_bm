import React,{ Component } from 'react';

class CommentApp extends Component{
    render(){ //render方法是必须的
        return(
            <div>
                CommentApp
            </div>
        )
    }
}

export default CommentApp;